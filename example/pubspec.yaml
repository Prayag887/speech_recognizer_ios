name: speech_recognization_example
description: "Demonstrates how to use the speech_recognization plugin."
publish_to: 'none'

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  speech_recognization:
    path: ../

  cupertino_icons: ^1.0.8
  permission_handler: ^11.4.0

dev_dependencies:
  integration_test:
    sdk: flutter
#  flutter_test:
#    sdk: flutter`

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true